import React from 'react';
import { useNavbar } from './NavbarContext'; // 경로는 적절하게 수정해주세요.

// const MoreButton = () => {
//   const { toggleNavbar } = useNavbar();

//   return <button onClick={toggleNavbar}>더보기</button>;
// };
